import { OrderDetailRoute } from '@/components/orders/OrderDetailScreen';

/** Full order detail — GET /restaurants/:id/orders/:orderId */
export default function OrderDetailPage() {
  return <OrderDetailRoute />;
}
